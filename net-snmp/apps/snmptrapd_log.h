
unsigned long format_plain_trap (char *,
				 unsigned long,
				 struct snmp_pdu *,
				 struct _snmp_transport *);


unsigned long format_trap (char *,
			   unsigned long,
			   const char *,
			   struct snmp_pdu *,
			   struct _snmp_transport *);
