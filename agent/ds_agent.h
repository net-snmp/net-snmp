#ifndef DS_AGENT_H
#define DS_AGENT_H
/* defines agent's default store registrations */

/* booleans */
#define DS_AGENT_VERBOSE 0         /* 1 if verbose output desired */
#define DS_AGENT_ROLE 1            /* 0 if we are master, 1 if client */

/* strings */
#define DS_AGENT_PROGNAME 0        /* argv[0] */

#endif
