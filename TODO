V3.1
  Gnu Auto-conf
    Makefiles
      sed script for man/mib/other
        mib file creation
    AC_FUNC_MEMCMP
  Solaris port (finish working out bugs...)
  Manual pages/Documentation

V3.2
  Pass off extensibles
  snmpset'able variables
  agent should send traps to a trap daemon.  
    (it might all ready, just needs configuration)

??? (3.2?)
  Define snmpv1 parties in the configuration file, acl.conf, and
    view.conf rather than being hard compiled in
