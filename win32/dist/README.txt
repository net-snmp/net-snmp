This dist directory contains items useful for producing releases.
However, only the main line CVS tree ("HEAD") for Net-SNMP contains
the maintained versions of all of these files.  They have been removed
from this directory.  Thus, if you would like to see them check out
the main line CVS tree per the instructions on:

  http://www.net-snmp.org/cvs/
